# platform-skills Sync Changelog

Append-only log of sync runs against
`https://gitlab.com/itential/platform-engineering/platform-claude-skills`.

Each entry is written by `scripts/sync-platform-skills.sh` when it runs.
Run `scripts/sync-platform-skills.sh --check` to see current staleness
without cloning.

<!-- entries appended below by sync script -->
